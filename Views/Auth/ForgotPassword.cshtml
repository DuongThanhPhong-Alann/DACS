@{
    ViewData["Title"] = "Quên mật khẩu";
}

<div class="login-form-container">
    <h2 class="text-center">Quên mật khẩu</h2>
    <form asp-action="ForgotPassword" method="post" class="login-form">
        <div class="form-group">
            <label for="email">Email</label>
            <input name="email" type="email" class="form-control" id="email" required placeholder="Nhập địa chỉ email của bạn" />
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-success">Gửi yêu cầu</button>
        </div>
    </form>
</div>

<!-- Styles for better appearance -->
<style>
    /* Container styling */
    .login-form-container {
        max-width: 100%;
        width: 400px; /* Set a max width for the form */
        margin: 50px auto;
        padding: 30px;
        background-color: #f8f9fa;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    h2 {
        color: #333;
        margin-bottom: 30px;
    }

    .form-group {
        margin-bottom: 20px;
    }

        .form-group label {
            font-weight: bold;
            margin-bottom: 8px;
            display: block;
        }

    .form-control {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 16px;
        box-sizing: border-box;
    }

        .form-control:focus {
            border-color: #28a745;
            box-shadow: 0 0 8px rgba(40, 167, 69, 0.5);
        }

    .btn {
        padding: 10px 20px;
        font-size: 16px;
        background-color: #28a745;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

        .btn:hover {
            background-color: #218838;
        }

    .text-center {
        text-align: center;
    }
</style>
